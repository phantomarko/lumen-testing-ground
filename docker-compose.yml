version: '3.8'

services:

  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    volumes:
      - ./.docker/mysql:/var/lib/mysql
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: yes
      MYSQL_USER: mysecureuser
      MYSQL_PASSWORD: mys3cur3p4ss
      MYSQL_DATABASE: lumen-test
    ports:
      - 33006:3306

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
